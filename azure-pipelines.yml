pool:
  vmImage: 'ubuntu-16.04'

container: node:10.16.0-jessie

steps:
- bash: du -sh .

- task: Cache@0
  inputs:
    key: $(Build.SourcesDirectory)/package.json
    path: node_modules

- bash: du -sh .

- bash: npm install